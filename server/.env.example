# Example .env for the proxy server
# Copy to `.env` and set your real token locally (do NOT commit `.env`)
# GITHUB_TOKEN=ghp_xxx_your_personal_token_here

# Optional GitHub OAuth App credentials (for OAuth demo)
# GH_OAUTH_CLIENT_ID=your_client_id
# GH_OAUTH_CLIENT_SECRET=your_client_secret
